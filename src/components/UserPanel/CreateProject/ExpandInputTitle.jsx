import React from "react";

const ExpandInputTitle = ({ title }) => {
  return (
    <div className="border rounded-xl p-6 mb-6 w-full">
      <p className="text-lg font-semibold">{title}</p>
    </div>
  );
};

export default ExpandInputTitle;
